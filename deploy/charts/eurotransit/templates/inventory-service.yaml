apiVersion: v1
kind: Service
metadata:
  name: {{ include "eurotransit.fullname" . }}-inventory
  labels:
    app.kubernetes.io/name: inventory
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.inventory.service.type }}
  ports:
    - port: {{ .Values.inventory.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: inventory
    app.kubernetes.io/instance: {{ .Release.Name }}

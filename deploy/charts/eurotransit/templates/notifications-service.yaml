apiVersion: v1
kind: Service
metadata:
  name: {{ include "eurotransit.fullname" . }}-notifications
  labels:
    app.kubernetes.io/name: notifications
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.notifications.service.type }}
  ports:
    - port: {{ .Values.notifications.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: notifications
    app.kubernetes.io/instance: {{ .Release.Name }}
